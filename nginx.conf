server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Hashed assets (Vite fingerprints filenames) — safe to cache for 1 year
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        gzip_static on;
        brotli_static on;
    }

    # Favicon — 1 week
    location = /favicon.png {
        add_header Cache-Control "public, max-age=604800";
    }

    # HTML shell — always revalidate so users get the latest deploy
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # SPA fallback — serves index.html for all routes (/, /en, /nl, /pt, /id, etc.)
    # so React Router can handle client-side navigation and language redirects
    location / {
        add_header Cache-Control "no-cache";
        try_files $uri $uri/ /index.html;
    }
}
